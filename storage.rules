rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // السماح الكامل بالقراءة والكتابة لجميع الملفات
    // ملاحظة: التطبيق يستخدم مصادقة مخصصة وليس Firebase Auth
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
