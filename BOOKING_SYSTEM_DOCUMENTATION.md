# نظام الحجوزات الجديد - التوثيق

## التحديثات الرئيسية

### 1. نظام التقويم التفاعلي 📅
- **تقويم ملون** يعرض حالة كل يوم:
  - 🟢 **أخضر**: اليوم متاح للحجز
  - 🟠 **برتقالي**: اليوم محجوز جزئياً (يمكن اختيار أوقات أخرى)
  - 🔴 **أحمر**: اليوم محجوز بالكامل (غير قابل للحجز)

### 2. أنواع الحجز
#### حجز اليوم كامل
- حجز العنصر لليوم بالكامل
- مناسب لـ: قاعات الأعراس، الفنادق

#### حجز فترة محددة
- اختيار وقت البدء ووقت النهاية
- مناسب لـ: الصالونات، السيارات، التصوير
- **منع التعارض**: لا يمكن اختيار أوقات متداخلة مع الحجوزات الموجودة

### 3. التحديثات في الوقت الفعلي (Real-time)
- **تحديث تلقائي** عند إضافة أو إلغاء حجز
- **جميع المستخدمين** يرون التحديثات فوراً
- استخدام **Firestore Streams** للاستماع المباشر

### 4. عرض الأوقات المحجوزة
عند الضغط على يوم محجوز جزئياً (برتقالي):
- تظهر نافذة منبثقة
- عرض جميع الفترات الزمنية المحجوزة (من - إلى)
- إمكانية اختيار وقت آخر لا يتعارض

### 5. شاشة إدارة الحجوزات للمزود
**المسار**: `lib/screens/provider_bookings_management_screen.dart`

#### الميزات:
- ✅ عرض جميع الحجوزات
- ✅ تفاصيل كل حجز (الزبون، التاريخ، الوقت، الملاحظات)
- ✅ إلغاء الحجز
- ✅ إرجاع التاريخ لحالة "متاح" تلقائياً
- ✅ تحديثات فورية عند الإلغاء

## الملفات الجديدة

### 1. CalendarService
**المسار**: `lib/services/calendar_service.dart`

**الوظائف**:
```dart
// الحصول على حالة الأيام (استماع مباشر)
Stream<Map<DateTime, DayStatus>> getMonthlyBookingStatus({
  required String itemId,
  required DateTime month,
})

// الحصول على الأوقات المحجوزة في يوم معين
Stream<List<TimeSlot>> getBookedTimeSlotsForDay({
  required String itemId,
  required DateTime date,
})

// التحقق من إمكانية الحجز
Future<bool> canBook({
  required String itemId,
  required DateTime date,
  TimeSlot? timeSlot,
})
```

### 2. شاشة إدارة الحجوزات
**المسار**: `lib/screens/provider_bookings_management_screen.dart`

**الوصول**: يجب إضافة زر في واجهة المزود للانتقال إلى هذه الشاشة

## التعديلات على الملفات الموجودة

### booking_screen.dart
**التغييرات الرئيسية**:
1. ✅ إضافة تقويم `TableCalendar` بدلاً من اختيار التاريخ العادي
2. ✅ إضافة اختيار نوع الحجز (كامل/جزئي)
3. ✅ إضافة اختيار وقت البدء والنهاية للحجز الجزئي
4. ✅ منع اختيار أوقات متعارضة
5. ✅ عرض نافذة الأوقات المحجوزة عند الضغط على يوم برتقالي
6. ✅ حفظ الحجز بالنموذج الجديد `BookingModel`

### booking_service.dart
**الوظائف الموجودة**:
- `createBooking()`: إنشاء حجز جديد
- `cancelBooking()`: إلغاء حجز (يعيد التاريخ لحالة متاح)
- `getProviderBookings()`: الحصول على حجوزات المزود
- `getCustomerBookings()`: الحصول على حجوزات الزبون

## كيفية الاستخدام

### للزبون:
1. فتح شاشة تفاصيل العنصر (قاعة، سيارة، إلخ)
2. الضغط على "حجز الآن"
3. اختيار التاريخ من التقويم الملون
4. اختيار نوع الحجز:
   - **اليوم كامل**: لحجز اليوم بالكامل
   - **فترة محددة**: لاختيار وقت البدء والنهاية
5. إذا كان اليوم محجوز جزئياً (برتقالي)، سيتم عرض الأوقات المحجوزة
6. رفع إيصال الدفع
7. تثبيت الحجز

### للمزود:
1. فتح شاشة إدارة الحجوزات
2. عرض جميع الحجوزات
3. الضغط على "إلغاء الحجز" لإرجاع التاريخ لحالة متاح
4. سيتم إشعار الزبون بالإلغاء (إذا كانت خدمة الإشعارات مفعّلة)

## التحسينات المستقبلية المقترحة

### إضافات محتملة:
1. **تعديل الحجز**: السماح بتعديل التاريخ أو الوقت
2. **إشعار الزبون**: إرسال إشعار عند إلغاء المزود للحجز
3. **تأكيد الحجز**: إضافة حالة "تأكيد" من المزود
4. **فلترة الحجوزات**: فلترة حسب التاريخ، الحالة، إلخ
5. **إحصائيات**: عرض إحصائيات الحجوزات للمزود

## المتطلبات

### الحزم المطلوبة (موجودة في pubspec.yaml):
```yaml
dependencies:
  table_calendar: ^3.0.9  # للتقويم
  intl: 0.20.2            # للتواريخ والأرقام
  cloud_firestore: ^5.4.4 # قاعدة البيانات
```

### قواعد Firestore Security Rules:
```javascript
match /bookings/{bookingId} {
  allow read: if true;
  allow write: if request.auth != null;
  allow delete: if request.auth != null;
}
```

## اختبار النظام

### سيناريو الاختبار:
1. ✅ إنشاء حجز كامل اليوم
2. ✅ التحقق من ظهور اليوم باللون الأحمر
3. ✅ محاولة حجز نفس اليوم (يجب أن تُرفض)
4. ✅ إنشاء حجز جزئي (مثلاً 10:00-14:00)
5. ✅ التحقق من ظهور اليوم باللون البرتقالي
6. ✅ الضغط على اليوم البرتقالي ورؤية الأوقات المحجوزة
7. ✅ محاولة حجز وقت متعارض (يجب أن تُرفض)
8. ✅ حجز وقت آخر غير متعارض (14:00-18:00)
9. ✅ إلغاء حجز من لوحة المزود
10. ✅ التحقق من إرجاع اليوم للون الأخضر (متاح)

## التواصل والدعم

في حالة وجود أي أسئلة أو مشاكل، الرجاء التواصل.

---

**تاريخ التحديث**: نوفمبر 2025
**الإصدار**: 2.0
